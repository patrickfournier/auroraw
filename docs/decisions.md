# Decision log

Decisions confirmed by Patrick, in order. A reversed decision is not deleted: it is marked
*superseded* and points to its replacement.

| # | Date | Decision |
| --- | --- | --- |
| D-001 | 2026-09-19 | **Audience**: digital photography is priority 1, film workflows are priority 2. |
| D-002 | 2026-09-19 | **Platforms**: Linux, Windows and macOS from the start. |
| D-003 | 2026-09-19 | **Video** is out of scope. Still images only. |
| D-004 | 2026-09-19 | **Collaboration**: single-user for now, no shared catalogue. |
| D-005 | 2026-09-19 | **Positioning**: simpler and more intuitive than darktable; at least on par with Lightroom in speed, simplicity and capabilities. |
| D-006 | 2026-09-19 | **Versions**: a photo can have several kept development versions; virtual copies, versions and snapshots are in v1. |
| D-007 | 2026-09-19 | **v1 scope**: fast culling, duplicates and series, versions, presets and styles, masks and local retouching, GPS, memory-card import, IPTC/XMP, catalogue backup and sync, batch export. *Backup and sync refined by D-064.* Lens corrections through Lensfun: v1, in milestone M3. |
| D-008 | 2026-09-19 | **Not in v1**: merges (HDR, panorama, focus stacking), soft proofing and printing, tethering. |
| D-009 | 2026-09-19 | **Prooftide, naming**: Auroraw generates a unique file name for each published version and keeps the mapping; no server change required. |
| D-010 | 2026-09-19 | **Prooftide, interoperability**: no interdependence. Auroraw stays compatible with other gallery services; Prooftide stays compatible with Lightroom and other workflows. Prooftide extensions for tighter integration with Auroraw are allowed if they are optional. |
| D-011 | 2026-09-19 | **Metadata**: ratings and keywords live on the photo, with an optional override on the version. |
| D-012 | 2026-09-19 | **Milestones**: M1 (catalogue and culling), M2 (basic development and versions), M3 (masks, lens, negatives), M4 (export, Prooftide, backup), M5 (public plugin API, AI, polish). |
| D-013 | 2026-09-19 | **License**: GPL-3.0 (made GPL-3.0-or-later by D-082). |
| D-014 | 2026-09-19 | **Keyword overrides**: in v1 a version can only add keywords to those of its photo, not remove them. |
| D-015 | 2026-09-19 | **Project language**: English for all repository documentation and code. Conversation with Patrick stays in French. |
| D-016 | 2026-09-19 | **Git workflow**: same rules as Prooftide. Work on `dev`, merge into `main` only at release. Claude Code commits as `Claude Code <claude-code@straycat.ca>`. |
| D-017 | 2026-09-19 | **Catalogues**: several catalogues, all equal, chosen at opening (typically separate areas of activity, such as family and professional). The application must be fully functional with a single catalogue. |
| D-018 | 2026-09-19 | **Originals are read-only** except at import: Auroraw does not modify, move, rename or delete them afterwards. Refined by D-022 and D-024. |
| D-019 | 2026-09-19 | **Change detection**: online sources are monitored; new files are reported and added with one click after confirmation; moved files are relinked by content fingerprint. |
| D-020 | 2026-09-19 | **Non-writable sources**: sidecars are written next to the original when possible, otherwise versions live in the catalogue only, with a warning; sidecars can be exported on demand. *Superseded by D-022.* |
| D-021 | 2026-09-19 | **No development on offline sources in v1.** A reduced-resolution proxy is not trusted to give the same result as the original. To be reconsidered once the pipeline exists. |
| D-022 | 2026-09-19 | **Workspace**: a folder, used by a single catalogue, that receives all sidecars (of originals and of versions) and optionally exports. Auroraw writes nothing into source folders except at import and on explicit XMP export. Supersedes D-020. |
| D-023 | 2026-09-19 | **Sidecar hierarchy**: the sidecar of an original holds only that original's metadata; the sidecar of a version copies those metadata and adds its own. |
| D-024 | 2026-09-19 | **Exports and XMP compatibility**: exports may go into the workspace or elsewhere, and exporting into a source folder is discouraged with a warning. An option exports the originals' XMP files into the source folders, for compatibility with other software. |
| D-025 | 2026-09-19 | **One workspace per catalogue**, exactly. |
| D-026 | 2026-09-19 | **Catalogue database is local** (user data folder) and rebuildable from the workspace and the sources. Everything that would otherwise live only in the database is also written into the workspace. |
| D-027 | 2026-09-19 | **Sidecar consistency**: when a photo's metadata changes, the copies in its version sidecars follow, except for the fields the version overrides. |
| D-028 | 2026-09-19 | **XMP naming** for the export to source folders is the photographer's choice (`photo.xmp` or `photo.ARW.xmp`), `photo.xmp` by default. |
| D-029 | 2026-09-19 | **Import profiles**: an import profile stores destination, naming, backup, metadata template and style; inserting a card offers a one-click import with a profile. Nothing starts automatically. |
| D-030 | 2026-09-19 | **Import everything**: no pre-selection at import; culling happens afterwards in the catalogue. |
| D-031 | 2026-09-19 | **The card is never modified**: Auroraw signals when the copies are verified and it is safe to erase, and deletes nothing. |
| D-032 | 2026-09-19 | **RAW+JPEG**: a pair is one photo with two files; the RAW is developed, the JPEG is a companion. A profile setting can ignore either kind. |
| D-033 | 2026-09-19 | **Cull mode**: a dedicated full-screen mode for culling, with the grid as the navigation view. |
| D-034 | 2026-09-19 | **Series formation**: objective series (bursts, bracketing) form automatically; visually similar photos are only suggested and the photographer confirms. |
| D-035 | 2026-09-19 | **Resolving a series** keeps the designated photos and gives the others the Rejected flag. The series stays expandable. Nothing is deleted. |
| D-036 | 2026-09-19 | **Exact duplicates** are one photo with several locations. An optional duplicates report helps the photographer tidy up; Auroraw itself deletes nothing. |
| D-037 | 2026-09-19 | **Develop interface**: guided panels by task, advanced tools added on demand, and a default pipeline that works out of the box. Operations are not reordered in everyday use. The pipeline must be configurable for advanced users and plugins, outside the everyday interface. |
| D-038 | 2026-09-19 | **Main version**: each photo has one main version (chosen by the photographer, by default the last edited) that supplies the grid thumbnail and the default for export and publication. Other versions are collapsed under the photo. |
| D-039 | 2026-09-19 | **Reusing settings**: selective copy and paste, styles, and an optional auto-sync switch. Application is one-off, with no lasting link between photos. |
| D-040 | 2026-09-19 | **One concept, the style**: a named set of settings on one or several tools; a tool preset is a single-tool style. |
| D-041 | 2026-09-19 | **Scene-referred core** (linear, floating point) with familiar controls exposed in the interface. |
| D-042 | 2026-09-19 | **Base look**: a style applied when a version is created; neutral by default, with flat linear among the choices. A look imitating the camera's embedded JPEG is a planned evolution. |
| D-043 | 2026-09-19 | **Local adjustments** are first-class objects (a mask plus its settings), listed, stackable and reorderable. |
| D-044 | 2026-09-19 | **Negatives, first inputs**: camera-scanned on a light table, dedicated film scanners, and third-party or lab scans. Flatbed files open as ordinary images. |
| D-045 | 2026-09-19 | **Keywords**: a hierarchical vocabulary with optional synonyms and a "do not export" flag per keyword, written into the workspace. |
| D-046 | 2026-09-19 | **Metadata in exports**: configurable per recipe with named settings; the default is everything except location and camera serial number. |
| D-047 | 2026-09-19 | **External XMP changes** are monitored like new files: reported, and accepted with one click after import. |
| D-048 | 2026-09-19 | **Geocoding**: a bundled offline database by default (GeoNames); finer online lookup only through an optional plugin, with consent. |
| D-049 | 2026-09-19 | **Publication tracking**: for every publication Auroraw records what was sent (photo, version, published name, date, settings). Modified photos are flagged; republishing is manual, never automatic. |
| D-050 | 2026-09-19 | **Client feedback**: each published gallery gets a "client selection" collection filled automatically, with an optional flag, label or keyword of the photographer's choice. Annotations are attached to the photo. |
| D-051 | 2026-09-19 | **Prooftide API**: the routes the plugin uses become a documented, versioned public API, usable by any client. |
| D-052 | 2026-09-19 | **Library scope**: export recipes, styles and import profiles live at user level, shared by all catalogues, and are importable and exportable as files. A publication keeps a copy of its recipe. |
| D-053 | 2026-09-19 | **Gallery revisions**: republishing a gallery creates a new revision (v2, v3...) rather than replacing photos. The whole collection is republished by default, with an option for only the modified and new photos. The revision is named after the first gallery with a suffix, such as "Marie wedding (v2)". |
| D-054 | 2026-09-19 | **Plugin distribution**: a catalogue built into the application, fed by a public open index with trust levels (official, verified, community), plus manual installation from a file or an address. No hosted store. |
| D-055 | 2026-09-19 | **Plugin trust**: sandboxed by default with declared permissions shown before installation; a native level is allowed, clearly flagged and needing explicit confirmation. |
| D-056 | 2026-09-19 | **Source plugins, first scenarios**: local disk folders and mounted cards (through the same interface), cameras and phones (PTP/MTP), and online storage. Scanner acquisition is not among the first scenarios. Importing from other software is kept for later. |
| D-057 | 2026-09-19 | **Plugin licensing is deferred** to the technical phase, together with the isolation model. Moving the core to the LGPL is an option kept open. **Resolved by D-080 and D-082.** |
| D-058 | 2026-09-19 | **First AI priorities (M5)**: selections for masks (subject, sky, people) and noise reduction, upscaling to follow. Assisted culling, semantic search and keyword suggestion are not prioritised for now. |
| D-059 | 2026-09-19 | **AI models** are downloaded on demand as packs through the plugin catalogue; the core ships none. |
| D-060 | 2026-09-19 | **AI without a capable GPU**: features run on the CPU, slowly, as background tasks with an estimated duration. |
| D-061 | 2026-09-19 | **Online AI**: the core calls no external AI service. Plugins may, off by default, with consent per plugin and a display of what leaves the machine. No telemetry. |
| D-062 | 2026-09-19 | **Minimal export in M2** (JPEG, TIFF, PNG; size, profile, metadata setting), so that development is usable on its own. Recipes, queue, watermark and publication stay in M4. |
| D-063 | 2026-09-19 | **Rating level**: Cull mode works on the photo's values, Develop on the version's values; the grid shows the effective value and marks a photo whose value is overridden. |
| D-064 | 2026-09-19 | **Backup and sync in v1**: a backup helper, rebuilding the catalogue from the workspace, and documented compatibility with file-sync tools. No built-in sync. |
| D-065 | 2026-09-19 | **XMP export to the source folders** ships in M1. |
| D-066 | 2026-09-19 | **No automatic XMP mirroring** in v1: the export to source folders is on request only. |
| D-067 | 2026-09-19 | **Moving photos between catalogues** is in v1, by copying sidecars between workspaces. Milestone M4 (confirmed). |
| D-068 | 2026-09-19 | **Core language**: Rust, for the catalogue, the pipeline and the plugin host. |
| D-069 | 2026-09-19 | **Interface priorities**: the performance and colour fidelity of the image view, and lightness (fast start, little memory). A web view (Tauri) is therefore not a candidate unless the native toolkits fail. |
| D-070 | 2026-09-19 | **The stack is fixed after short spikes** on four risks (the GPU pipeline, the image view and toolkit, the catalogue and grid, the plugin sandbox), on measurements. See technical-spikes.md. |
| D-071 | 2026-09-19 | **Test platforms**: Linux on the development machine, Windows by Patrick, macOS by Patrick over remote desktop. Continuous integration builds all three. Colour is judged on Linux and Windows, not over remote desktop. |
| D-072 | 2026-09-20 | **Superseded by D-094 (2026-09-24).** **Interface toolkit: Slint.** Qt Quick stays as the documented fallback if Slint shows a blocking problem; iced is ruled out (no accessibility). A developed image reaches the toolkit as an RGBA buffer, with no GPU sharing. Two checks remain, not blocking: Slint's rendering on macOS, and DirectX 12 on a real GPU. See docs/spikes/02-image-view-and-toolkit.md. |
| D-073 | 2026-09-20 | **Catalogue database**: SQLite, with the effective rating stored in the photo table (kept in step with the main version's override, D-063) and a full-text index. Measured at 100,000 photos, every query is far under budget. |
| D-074 | 2026-09-20 | **Photo sidecars cache the original's metadata** (capture time, camera, lens, exposure, size, GPS) so that rebuilding the catalogue from the workspace (D-026) does not reopen the originals. This makes the rebuild take seconds instead of minutes, and it is the only way to rebuild with an offline source. |
| D-075 | 2026-09-20 | **Thumbnails**: a separate SQLite database with 32 KB pages, per catalogue, in the cache (deletable without loss); files stay the fallback. **Provisional** until the Windows run of spike 3 shows how NTFS and antivirus behave. |
| D-076 | 2026-09-20 | **Plugin sandbox: WebAssembly (wasmtime)** for operation and import plugins, with explicit permissions, per-plugin limits on memory and time, and a cache of compiled plugins made at installation. Decoders may ask for the native level (D-055). Measured at 1.2 to 2 times native, with identical results. See docs/spikes/04-plugin-sandbox.md. |
| D-077 | 2026-09-20 | **GPU operation plugins** ship their shader as data, with a CPU twin in WebAssembly. The host validates the shader, places the operation in the pipeline from its declaration, and refuses it if it is invalid. A safety and portability check (loops proven bounded, tested on every graphics API) is mandatory before a plugin is admitted to the index. |
| D-078 | 2026-09-20 | **Plugin declaration**: every plugin carries a declaration (identifier, version, API version, family, panel, pipeline stage and ordering constraints, parameters with limits and defaults, permissions). The host refuses a declaration it cannot satisfy, with the reason. |
| D-079 | 2026-09-20 | **Architecture adopted** as written in docs/architecture.md: the module layout and its dependency rules (the interface talks only to the engine; the pipeline, format and catalogue run headless), the single-process threading model with one writer, commands and events, the write path (sidecar first, then database, with reconciliation), and the plan of what arrives in each milestone. Items marked open in §14 stay open. |
| D-080 | 2026-09-20 | **Licensing**: the application stays under **GPL-3.0** (D-013 confirmed). Moving the core to the LGPL was considered and dropped: the interface toolkit (Slint, D-072) is available only under the GPL-3.0, a royalty-free licence with attribution conditions, or a commercial one, and the royalty-free terms sit uneasily with the LGPL. The **plugin API, the plugin SDK and the declaration schema are licensed MIT OR Apache-2.0**, so that anyone can write a plugin, free or not. The licence of the plugins themselves and the plugin exception are dealt with in docs/governance.md (D-057 stays open until they are approved). **Tie-break: if any licensing question turns out to contradict the GPL-3.0, everything is GPL-3.0.** |
| D-081 | 2026-09-20 | **Testing and release plan adopted** (docs/testing-strategy.md, docs/continuous-integration.md): the test levels, the CI workflows, branches, real-GPU checks through an `xtask` command, supply-chain rules. **Signing**: Windows through SignPath (or an equivalent), macOS through the Apple Developer account Patrick already holds; the first 0.x pre-releases may be unsigned. **Updates**: no automatic check; a "Check for updates" command and an opt-in setting that fetches one small file; no automatic download or install in v1. The repository is public. |
| D-082 | 2026-09-20 | **Governance adopted** (docs/governance.md). **Contributions**: the Developer Certificate of Origin (`Signed-off-by` on every commit, checked on pull requests); commits made by Claude Code carry the owner's sign-off. **Licence**: GPL-3.0-or-later. **Plugins** (resolves D-057): a plugin that uses only the plugin API may have any licence, stated in an additional permission under section 7 of the GPL (`PLUGIN-EXCEPTION`, to be reviewed by a qualified person before the first release); the tie-break of D-080 applies. **Language**: everything, including discussions, is in English. **Translations**: `.po` files sent as pull requests (Qt Linguist `.ts` files since D-094). **Conduct**: Contributor Covenant 2.1, reports to auroraw-conduct@straycat.ca. **Security**: private vulnerability reporting on. **Funding**: GitHub Sponsors button. |
| D-083 | 2026-09-20 | **Documentation licence**: CC BY-SA 4.0 for everything in `docs/`. The project name was checked briefly and the domain **auroraw.org** is registered by the owner. |
| D-084 | 2026-09-20 | **Milestone M1 plan adopted** (docs/m1-plan.md): twelve work packages in four increments. **Previews** are converted to sRGB and displayed as sRGB in M1; reading the display profile and full colour management come with the pipeline in M2. **The map view is postponed** beyond M1 (it needs map tiles, hence a network request); GPS reading, GPX matching and offline place names stay in M1. The answers proposed in §6 of the plan are each approved when their design note comes up. |
| D-085 | 2026-09-21 | **Workspace layout adopted** (docs/design/001-workspace-layout.md): `photos/<xx>/<photo id>.xmp` and `versions/<xx>/<photo id>.<version id>.xmp` with 256 shards created on demand; random identifiers (128 bits for a photo, 64 for a version) and identifier-only file names; a `workspace.json` marker with a layout version and workspace identifier; atomic writes through `.auroraw/tmp/` with a retry on Windows; one writer under an advisory lock; unknown files left alone and reported; the default workspace at `<Pictures>/Auroraw/<catalogue name>`, the default catalogue name being a translated string; names inside the workspace never translated. |
| D-086 | 2026-09-21 | **State files adopted** (docs/design/002-state-files.md): one JSON file per collection and per series (series sharded), the vocabulary and the sources as single files; an envelope with format, schema, identifier and update time; canonical writing, so an unchanged file is not rewritten; unknown content preserved; 64-bit identifiers for state entities; machine-specific data (real source locations, view state) kept in the local registry and never in the workspace; missing photos kept and marked; deleted entities moved to `removed/state/`; a newer schema is never modified; conflicted copies from sync tools left alone and reported. |
| D-087 | 2026-09-21 | **Sidecars adopted** (docs/design/003-sidecars.md): XMP written and read by Auroraw's own code (a strict canonical subset for writing, the general form for reading, unknown content preserved); the properties of the photo sidecar and the version sidecar, in the `https://auroraw.org/ns/1.0/` namespace (`aur`); stars and flag kept apart, converted only at the boundaries, with an exported rejected photo also carrying `aur:Flag` and `aur:Stars`; the photo sidecar plus the version's overrides is the truth and the copy in a version sidecar is derived, refreshed in the background and repaired by a digest; keywords identified by identifier, with the names in sidecars only a snapshot (a rename writes no sidecar); the metadata fields of M1; the mapping at each boundary, including the merge into an existing XMP file at export (three-way against a locally stored base, replace moves the old file to `removed/`). |
| D-088 | 2026-09-21 | **Content fingerprint adopted** (docs/design/004-fingerprint.md): two levels, a **sampled fingerprint** (`sampled-v1`, BLAKE3 over the size and 64 KB from the start, middle and end) for finding files (relinking, duplicates, change detection) and a **whole-file BLAKE3 hash** for every decision that can lose data. **Import skips a file, and copies are verified, only on the whole-file hash, never on the fingerprint alone.** Relinking is silent only when the match is unique and the old location is gone with its source online; a source that is offline never makes a file "gone"; a file edited elsewhere is only proposed as a match, never linked automatically; duplicates are merged on the fingerprint and confirmed by the hash in the background, with an undo; cloud placeholders are never read; the whole-file hash of existing files is a throttled background job. Size, fingerprint and hash are recorded in the `aur:Files` entries of the photo sidecar. |
| D-089 | 2026-09-23 | **Vision against Lightroom refined**: as simple to use for everyday work, with room for **advanced development pipelines** (depth is the same model, reached on demand, with no expert mode that changes the everyday interface), and **no round trip between the cloud and the local machine**: every operation of the core, AI features included, runs on the photographer's machine on files it can read; online storage stays a possible source and delivery to a gallery service stays an explicit action (D-061), but neither is a step development depends on. An **advanced pipeline** means the configurable pipeline definition of the spec's §5.6 (D-037), nothing more. How originals on online storage reach the machine stays open (spec §10, question 41). |
| D-090 | 2026-09-23 | **Workflow**: the first launch offers to **create a workspace or pick a known one** (a welcome list, JetBrains-style; amends D-017's "creates one silently"); later launches **reopen the last workspace**, and show the welcome list when it cannot be found; a **new** workspace opens on the **catalogue panel**, an existing one on the **grid**. The interface has a **hamburger menu**: File (New workspace, Open workspace, Settings, Import, Quit), Edit (Undo, Redo, Cut, Copy, Paste, Select all, Delete; on text fields for now, Delete never deletes photos), Help (About); Settings holds the language only. **Import is a function, not a task**: a dialog opened from File or from a card's notification, separate from the catalogue. The **Catalogue** task replaces Import: it adds and removes **sources** (folders, local or network; cloud through plugins later). Adding a source copies nothing, it scans and builds the sidecars; network and cloud originals are **copied on demand when developed**. The catalogue database and previews live in the machine's data and cache folders, not in the workspace (D-026, D-075, design note 001 §5.7). |
| D-091 | 2026-09-23 | **Removing a source** takes its photos out of the catalogue **recoverably**: their sidecars and versions move to the workspace's `removed/`, the confirmation gives the numbers (photos, and how many carry ratings, keywords, titles or versions), originals are never touched, a photo that has another location keeps it. **Adding a source again offers to restore** the photos `removed/` holds for its files (matched by content fingerprint), with their ratings and versions, or to add the files as new photos. |
| D-092 | 2026-09-23 | **Paths are canonical**: every path a person types or picks is resolved once (quotes, `~`, `$VAR`, `%VAR%` on Windows, `.`, `..`, repeated separators, symlinks for the part that exists, the Windows verbatim prefix; a relative path is refused). **Sources may be nested**: a folder is a source when it is, or lies inside, a registered source (a photo's location is the covering source and the path inside it); a folder inside a source is not added again; a folder containing sources may **merge** them (their photos are kept and re-parented); a scan never descends into another source's folder. |
| D-093 | 2026-09-23 | **Import layout and names**: a card with camera folders (`DCIM/100CANON/...`) offers to **keep its folders** (below `DCIM`, names as found, **merged** into a folder that exists, numbering only a clash) **or to use the template**; new template tokens `{path}`, `{folder}`, `{name}`; **imported files never change case**, extension included (`{ext}` no longer lower-cases), and name collisions are compared without case; a file already at its exact place with the same content is skipped; only still images are copied. |
| D-094 | 2026-09-24 | **Interface toolkit: Qt Quick, through cxx-qt** (supersedes D-072). After spike 5 (docs/spikes/05-qt-quick-through-rust.md): the first screens rebuilt on Qt Quick need 61 lines of C++, rebuild in about 9 s, have real modal dialogs, menus and Edit actions from the toolkit, and build and pass 14 offscreen tests with real input events on Linux, Windows and macOS in CI with Qt 6.8.3 (Qt 6.4.2 builds too). **Style: Fusion on every platform, no native styles**, with a neutral grey theme of our own and colour accents where they do not disturb. **Translations: Qt Linguist `.ts` files**, compiled by `lrelease` (replaces the gettext `.po` of D-082). **`unsafe`** is allowed in the interface crate only, in its FFI modules, each block with a `SAFETY` comment; the workspace lint stays `deny` elsewhere. The hamburger menu of D-090 stays. The Slint shell stayed on `dev` until the Qt one reached parity (its 54 headless scenarios ported, three platforms green, a manual pass on Linux; Windows left to CI by Patrick's decision) and was then deleted (2026-09-25, tag `slint-shell-final`). Qt is used under the LGPL-3.0, linked dynamically into the GPL-3.0-or-later application: the application's licence does not change, and D-080's reason for keeping the core off the LGPL (Slint's terms) no longer holds but is not reopened here. Qt Bridge for Rust (Qt's own, in beta, needs Qt 6.10 or later) is left out until it leaves the beta. |
| D-095 | 2026-09-25 | **Interface typeface: IBM Plex Sans at 11 points**, carried by the application (Regular, Italic, Bold and Bold Italic, about 0.6 MB, `crates/ui/assets/fonts/`, from Google Fonts) so that it looks the same on every platform. Licence: SIL Open Font License 1.1, copied beside the files and credited in the About dialog; the fonts are embedded in the executable, not modified, and not sold on their own. `AURORAW_FONT` and `AURORAW_FONT_SIZE` still try another typeface without a rebuild. |
| D-096 | 2026-09-26 | **Undo and redo of what a person does to their photos: a command journal in the engine.** The single writer records every edit of a photo (rating, flag, keywords) as a **before and after pair** (a never-rated photo goes back to *unset*), one **entry** per action however many photos it touches (`Command::Batch`, all or nothing); `Command::Undo` and `Command::Redo` apply the pair in the other direction, a new edit discards what was undone (spec §5.4), a same-value edit is not a step, a step about a photo that has gone is dropped. **In memory, per open workspace, at most 500 steps**: the persistent history of a *version* (M2, in the sidecar) is a different mechanism. An undo restores what the person did, absolutely: an edit made outside the application in between (Reconcile) is not treated as a conflict. Not journaled yet, and not undoable: import, adding or removing a source. (Keyword vocabulary edits were left out here and are journaled since D-099.) The interface names the step in the Edit menu ("Undo rating"), leaves a focused text field its own undo, disables photo undo behind a dialog, and shows the photo undone. |
| D-097 | 2026-09-26 | **Selecting several photos in the grid**, as in a file manager. Three things: the **selection** (a set of photos, kept **by identifier** in `PhotoGrid` so that a reload or a photo arriving does not lose it; a new filter clears it), the **cursor** (where the keyboard is: the `GridView`'s current index) and the **anchor** (where a range starts). Click selects only that photo; Ctrl+click toggles one; Shift+click selects the range from the anchor, Ctrl+Shift+click adds it; the arrows, Home, End, PageUp and PageDown do the same to the photo they land on (Shift extends the range, Ctrl moves the cursor and leaves the selection); Space toggles the cursor's photo, Escape clears, a drag is a rubber band (Ctrl adds). Edit: Select all (Ctrl+A, the text field's when one has the keyboard), Select none (Ctrl+Shift+A), Invert selection (Ctrl+Shift+I). The rating keys rate the whole selection as one `Command::Batch`, one step of the history (D-096), and an undo selects the photos it touched. The selection is the object flags, keywords and series actions will act on. Rating very many photos in one gesture is one large batch on the coordinator; a background batch job with progress is left to WP10. |
| D-098 | 2026-09-26 | **Flags and the keyword panel in the interface.** **Flags**: P (Picked) and X (Rejected) **toggle**: the same key on a photo that has the flag takes it off, and on a selection the flag is taken off when every photo has it, set on all otherwise (only the photos that change are in the step); U clears. One flag or one `Command::Batch`, one step of the history. A **rejected photo stays in place, dimmed, until the list is read again** (a filter, a reload), so that the grid does not shift under the cursor while culling; nothing is ever deleted (§5.3). The grid's list is one composed catalogue filter (`Filter`: minimum rating, flag view, keyword with its descendants; `Catalogue::list_filtered`), and the flag view is *Not rejected* (default), *All*, *Picked* or *Rejected*. **Keywords**: a panel on the right of the grid (280 px by default, resizable by dragging its edge (200 to 640 px, double-click restores it, the width is remembered in `app-settings.json`), collapsible, the first of the inspector's panels): the vocabulary as a tree with the number of photos, a check per keyword that says whether the selected photos carry it **none, some or all** (a click gives it to all or takes it off all: one batch, one undo step), a field that filters the tree as one types (Enter gives the best match, and makes the keyword when nothing matches; Shift+Enter makes it even when something does; a keyword clicked first becomes the parent of the next new ones), Ctrl+K puts the keyboard in the field and Escape gives it back to the grid, a context menu (show the photos with this keyword, rename). Creating and renaming a keyword were left out of the history here, which D-099 reverses, and D-099 adds moving and deleting with drag and drop. **Not in this pass**: colour labels (no engine command), the Rejected view's "show in file manager" and export list, moving to the next photo after a flag (WP9), synonyms, the do-not-export flag, merging keywords (WP10), collections, and the metadata panel (WP10). |
| D-099 | 2026-09-26 | **The vocabulary is edited as the photos are: every edit is an action of the person's, is undone and redone, and can be reorganised by drag and drop.** *Journal*: making, renaming, moving and deleting a keyword are steps of the history (D-096), each a `Change::Vocabulary` holding the entries that changed, before and after, so that undo, redo and the first time are the same operation (`apply_vocabulary`: the vocabulary file, the catalogue's keyword rows and paths, and the sidecars' path snapshots). Typing a new keyword with photos selected is **one** step (`Batch` of `CreateKeyword`, with an identifier the caller draws, and the assignments). *Delete* takes the keyword **and its branch**: the photos that carry any of it lose it first (one change per photo), then the vocabulary loses the entries, so that a rebuild never meets a sidecar naming a keyword that is gone; undo brings back the same keywords with the same identifiers (an identifier is never given to another keyword, note 003 §6) and the same photos, all or nothing, and a photo that left with a removed source meanwhile is skipped rather than blocking the undo. The confirmation says how many keywords and photos it takes. *Move*: under another keyword or to the top level, not under itself or one of its own; *rules for names* (engine, so every caller): not empty, no `|`, no two siblings with the same name whatever the case. *Interface*: a keyword is dragged onto another (or onto a strip that appears for the top level), or moved from its menu (*Move to…*, *Move to the top level*); a step about the vocabulary does not select photos when undone. *Concurrency*: the background job that refreshes the sidecars' path snapshots now runs **one at a time in the order asked** (an undo right after a rename ends with the old paths), and every read-modify-write of one photo's files (the coordinator's edits, that job, a source removal, an index merge) holds `Workspace::sidecar_guard()`; before, a refresh that had read a sidecar just before a source removal moved it away wrote it back into `photos/` (a photo the catalogue no longer has, which a rebuild would bring back), and two writers could overwrite each other. The catalogue row of a refreshed photo is now read again from its sidecar on the coordinator instead of taken from the job's copy. *Limits*: deletion runs on the coordinator like a batch of ratings (a background job with progress for a very large branch is WP10's, as note 003 §6 says); a deleted keyword is not taken off *versions'* added keywords (nothing writes them in M1); undoing a delete after a same-named keyword was made beside it leaves two siblings with the name (the identifiers differ). Merging, synonyms and the do-not-export flag stay WP10. |
