# The plugins of spike 4, built for WebAssembly:
#   cargo build --release --target wasm32-wasip1
[workspace]
resolver = "3"
members = ["kernels", "ops", "hostile", "rawimport", "gpuop"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
